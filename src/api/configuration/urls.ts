import { constants } from "@/constants/index";

export const config = {
  GetCallBackURLData: `${constants.baseUrl}User/GetCallBackURLData`,
  AddUpdateCallBackURL: `${constants.baseUrl}User/AddUpdateCallBackURL`,
  AddRechargeIP: `${constants.baseUrl}User/AddRechargeIP`
};